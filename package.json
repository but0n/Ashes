{
  "name": "Ashes",
  "version": "0.0.1",
  "description": "An 3D engine for indie games",
  "main": "index.js",
  "repository": "https://github.com/but0n/Ashes.git",
  "author": "but0n <but0n@icloud.com>",
  "license": "MIT",
  "dependencies": {
    "@types/webgl2": "^0.0.4",
    "gl-matrix-ts": "^2.3.3",
    "http-server": "^0.11.1",
    "rollup": "^0.66.6",
    "rollup-plugin-typescript2": "^0.17.2",
    "typescript": "^3.1.3"
  },
  "devDependencies": {
    "madge": "^3.2.0"
  },
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "rollup -c && madge ./build --circular",
    "dev": "http-server -o -s -c-1 | rollup -cw"
  }
}
